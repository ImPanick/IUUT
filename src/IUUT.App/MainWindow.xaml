<ui:FluentWindow x:Class="IUUT.App.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:vm="clr-namespace:IUUT.App.ViewModels"
                 xmlns:views="clr-namespace:IUUT.App.Views"
                 mc:Ignorable="d"
                 Title="Icarus Ultimate Utility Tool (IUUT)"
                 Width="1120"
                 Height="740"
                 MinWidth="920"
                 MinHeight="620"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="None"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterScreen"
                 FontFamily="{StaticResource MonoFontFamily}"
                 Background="{StaticResource OrbitBackgroundBrush}">

    <!--
        Glass Console navigation shell (UI-DESIGN-CONCEPT §8). Frameless WPF-UI chrome + the
        in-window orbit background; a content region swaps page view-models (Home/Recovery/…)
        via implicit DataTemplates. No logic here — DataContext is the ShellViewModel.
    -->

    <ui:FluentWindow.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RecoveryViewModel}">
            <views:RecoveryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CustomViewModel}">
            <views:CustomView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GameTunerViewModel}">
            <views:GameTunerView />
        </DataTemplate>
    </ui:FluentWindow.Resources>

    <Grid>
        <!-- Faint 1px "chart" grid backdrop behind everything (opaque panels sit on top) -->
        <Rectangle Fill="{StaticResource GridBackdropBrush}" IsHitTestVisible="False" />

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ui:TitleBar Grid.Row="0" Title="IUUT · Icarus Ultimate Utility Tool">
                <ui:TitleBar.Icon>
                    <ui:ImageIcon Source="{StaticResource AppGlyphImage}" Width="16" Height="16" />
                </ui:TitleBar.Icon>
            </ui:TitleBar>

            <!-- Nav bar — hidden on Home, shown on sub-pages -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,4,20,0">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CanGoBack}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Button Content="←  BACK" Command="{Binding BackCommand}" Margin="0,0,8,0" />
                <Button Content="HOME" Command="{Binding HomeCommand}" />
            </StackPanel>

            <ContentControl Grid.Row="2" Content="{Binding CurrentPage}" Margin="20,8,20,16" />
        </Grid>
    </Grid>
</ui:FluentWindow>
