<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        IUUT "Telemetry Console" control layer (docs/UI-DESIGN-CONCEPT.md v2 §7, §9).
        Merged AFTER WPF-UI's dictionaries so these overrides win. Two jobs:
          1. Repoint WPF-UI's Fluent theme resource keys to the graphite palette + 2px corners,
             so ComboBox / TextBox / CheckBox / Slider / ScrollBar recolour without re-templating.
          2. Provide explicit graphite Button styles (ghost default + Primary + Danger).
        Token values mirror GlassTheme.xaml (doc §2). If a WPF-UI key name is absent in this
        version it simply goes unused — harmless.
    -->

    <!-- ============ Corner radius (sharpen everything to 2px) ============ -->
    <CornerRadius x:Key="ControlCornerRadius">2</CornerRadius>
    <CornerRadius x:Key="OverlayCornerRadius">2</CornerRadius>

    <!-- ============ Accent ============ -->
    <Color x:Key="SystemAccentColor">#FF0A84E0</Color>
    <Color x:Key="SystemAccentColorPrimary">#FF0A84E0</Color>
    <Color x:Key="SystemAccentColorSecondary">#FF1A8FEF</Color>
    <Color x:Key="SystemAccentColorTertiary">#FF3AA0F2</Color>
    <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="#FF0A84E0" />
    <SolidColorBrush x:Key="AccentFillColorSecondaryBrush" Color="#FF1A8FEF" />
    <SolidColorBrush x:Key="AccentFillColorTertiaryBrush" Color="#FF2A97F0" />
    <SolidColorBrush x:Key="AccentFillColorDisabledBrush" Color="#FF2E2C29" />
    <SolidColorBrush x:Key="SystemFillColorAttentionBrush" Color="#FF0A84E0" />
    <SolidColorBrush x:Key="AccentTextFillColorPrimaryBrush" Color="#FF6BB8F0" />
    <SolidColorBrush x:Key="AccentTextFillColorSecondaryBrush" Color="#FF8FCAF4" />
    <SolidColorBrush x:Key="TextOnAccentFillColorPrimaryBrush" Color="#FF06121F" />
    <SolidColorBrush x:Key="TextOnAccentFillColorSecondaryBrush" Color="#FF0B1A28" />

    <!-- ============ Text ============ -->
    <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#FFECEAE7" />
    <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="#FF9C988F" />
    <SolidColorBrush x:Key="TextFillColorTertiaryBrush" Color="#FF74706A" />
    <SolidColorBrush x:Key="TextFillColorDisabledBrush" Color="#FF5A574F" />

    <!-- ============ Control fills (button / textbox / combobox bodies) ============ -->
    <SolidColorBrush x:Key="ControlFillColorDefaultBrush" Color="#FF2B2926" />
    <SolidColorBrush x:Key="ControlFillColorSecondaryBrush" Color="#FF353230" />
    <SolidColorBrush x:Key="ControlFillColorTertiaryBrush" Color="#FF232120" />
    <SolidColorBrush x:Key="ControlFillColorDisabledBrush" Color="#FF1F1D1C" />
    <SolidColorBrush x:Key="ControlFillColorInputActiveBrush" Color="#FF1A1816" />
    <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="#FF3C3934" />
    <SolidColorBrush x:Key="ControlStrokeColorSecondaryBrush" Color="#FF4A463F" />
    <SolidColorBrush x:Key="ControlStrokeColorOnAccentDefaultBrush" Color="#FF1A8FEF" />
    <SolidColorBrush x:Key="ControlElevationBorderBrush" Color="#FF3C3934" />
    <SolidColorBrush x:Key="CircleElevationBorderBrush" Color="#FF3C3934" />

    <!-- ============ Subtle fills (ghost hover / list-item hover) ============ -->
    <SolidColorBrush x:Key="SubtleFillColorTransparentBrush" Color="Transparent" />
    <SolidColorBrush x:Key="SubtleFillColorSecondaryBrush" Color="#FF353230" />
    <SolidColorBrush x:Key="SubtleFillColorTertiaryBrush" Color="#FF2B2926" />
    <SolidColorBrush x:Key="SubtleFillColorDisabledBrush" Color="Transparent" />

    <!-- ============ Cards / layers / solid backgrounds (popups, flyouts) ============ -->
    <SolidColorBrush x:Key="CardBackgroundFillColorDefaultBrush" Color="#FF232120" />
    <SolidColorBrush x:Key="CardBackgroundFillColorSecondaryBrush" Color="#FF2B2926" />
    <SolidColorBrush x:Key="CardStrokeColorDefaultBrush" Color="#FF3C3934" />
    <SolidColorBrush x:Key="LayerFillColorDefaultBrush" Color="#FF232120" />
    <SolidColorBrush x:Key="LayerFillColorAltBrush" Color="#FF2B2926" />
    <SolidColorBrush x:Key="LayerOnAcrylicFillColorDefaultBrush" Color="#FF232120" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorBaseBrush" Color="#FF131211" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorSecondaryBrush" Color="#FF1A1816" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorTertiaryBrush" Color="#FF232120" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorQuaternaryBrush" Color="#FF2B2926" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorBaseAltBrush" Color="#FF0E0D0C" />
    <SolidColorBrush x:Key="ApplicationBackgroundBrush" Color="#FF131211" />
    <SolidColorBrush x:Key="AcrylicBackgroundFillColorDefaultBrush" Color="#FF1A1816" />
    <SolidColorBrush x:Key="AcrylicBackgroundFillColorBaseBrush" Color="#FF1A1816" />
    <SolidColorBrush x:Key="FlyoutBackgroundBrush" Color="#FF1A1816" />

    <!-- ============ Focus / dividers ============ -->
    <SolidColorBrush x:Key="FocusStrokeColorOuterBrush" Color="#FF0A84E0" />
    <SolidColorBrush x:Key="FocusStrokeColorInnerBrush" Color="#FF06121F" />
    <SolidColorBrush x:Key="DividerStrokeColorDefaultBrush" Color="#FF3C3934" />
    <SolidColorBrush x:Key="CardStrokeColorDefaultSolidBrush" Color="#FF3C3934" />

    <!-- ============ ScrollBar ============ -->
    <SolidColorBrush x:Key="ScrollBarTrackFillColorBrush" Color="#FF1A1816" />
    <SolidColorBrush x:Key="ScrollBarThumbFillColorBrush" Color="#FF4A463F" />

    <!-- =======================================================================
         Buttons — explicit graphite styles. Implicit Button = ghost.
         ======================================================================= -->

    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource MonoFontFamily}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource TextHiBrush}" />
        <Setter Property="Background" Value="{StaticResource SurfaceRowBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource SurfaceRowHoverBrush}" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource GridLineStrongBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource SurfaceRaisedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.45" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Implicit: every plain Button is a ghost button -->
    <Style TargetType="Button" BasedOn="{StaticResource GhostButton}" />

    <!-- Primary action (Apply / Repair) — accent fill -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource MonoFontFamily}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource AccentTextOnBrush}" />
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,7" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentHiBrush}" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentHiBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.4" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Danger (destructive) — red ghost → red fill on hover -->
    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Foreground" Value="{StaticResource StateDangerBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{StaticResource StateDangerBrush}" />
    </Style>

</ResourceDictionary>
