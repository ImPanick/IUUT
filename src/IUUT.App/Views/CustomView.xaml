<UserControl x:Class="IUUT.App.Views.CustomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:IUUT.App.ViewModels"
             xmlns:views="clr-namespace:IUUT.App.Views"
             mc:Ignorable="d">

    <!-- Editor panels are picked by the view-model type of CurrentEditor. -->
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:AccountEditorViewModel}">
            <views:AccountEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PlaceholderEditorViewModel}">
            <views:PlaceholderEditorView />
        </DataTemplate>
    </UserControl.Resources>

    <!-- Custom editor shell (master §10.3): profile selector + category sidebar → editor panel. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Profile selector -->
        <Border Grid.Row="0" Style="{StaticResource GlassCard}" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="PROFILE" Style="{StaticResource LabelText}" VerticalAlignment="Center" Margin="0,0,12,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding Slots}" SelectedItem="{Binding SelectedSlot, Mode=TwoWay}"
                          DisplayMemberPath="DisplayLabel" Margin="0,0,10,0" />
                <Button Grid.Column="2" Content="Reload" Command="{Binding LoadSavesCommand}" Padding="14,4" />
            </Grid>
        </Border>

        <!-- Sidebar + editor panel -->
        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource GlassCard}" Padding="6" Margin="0,0,12,0">
                <ListBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4,7">
                                <TextBlock Text="{Binding Glyph}" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Label}" Style="{StaticResource BodyText}" VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <ContentControl Grid.Column="1" Content="{Binding CurrentEditor}" />
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Style="{StaticResource MutedText}" Margin="2,10,0,0" TextWrapping="Wrap" />
    </Grid>
</UserControl>
