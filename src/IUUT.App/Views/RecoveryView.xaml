<UserControl x:Class="IUUT.App.Views.RecoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <!-- Stub — wired to the recovery services in the next step. -->
    <Border Style="{StaticResource GlassCard}" Padding="24" VerticalAlignment="Top">
        <StackPanel>
            <TextBlock Text="🚑  Broken Save Recovery" Style="{StaticResource TitleText}" />
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource MutedText}" Margin="0,10,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </Border>
</UserControl>
